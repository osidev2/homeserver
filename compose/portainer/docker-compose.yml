version: "3.3"

services:

  portainer:
    image: portainer/portainer-ce:2.0.0
    command: -H unix:///var/run/docker.sock
    restart: always
    ports:
      - "9000:9000"
    networks:
      - traefik  
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    labels:
      # Frontend
      - "traefik.enable=true"
      - "traefik.http.routers.portainer.rule=Host(`port.ositech.nl`)"
      - "traefik.http.routers.portainer.entrypoints=web"


volumes:
  portainer_data:

networks:
  traefik:
    external:
      name: traefik
  default:
    driver: bridge